# Results highlight (case study)

Source: `.over-overlay` heading + `.outer-wrap-white{background:#fff}` card, `.flex-wrap` 2-column split, big stat number `.xl{font-size:2.5em;font-weight:600;line-height:1.1}`, testimonial text-slider nested inside the left column in the source (simplified out here to keep the section focused on the single stat/case-summary + CTA).

- White rounded card (radius 22px) over the gray page background, stat + case copy + CTA on the left, tall portrait/device image slot on the right.
