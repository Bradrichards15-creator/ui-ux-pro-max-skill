# Hero

Source: `.hero-intro .line-text{font-size:15vw;line-height:1.1;font-weight:600}` scaling to `7-8vw` at desktop widths; `.rounded{border-radius:30px;background:#82FFCD;font-size:9-13px;padding:7-16px}`; `.line-video{border-radius:12px}`.

- 3 stacked lines, alternating left/right emphasis (`anim-left`/`anim-right`), each pairing headline text with a small rounded video slot and/or a floating green pill stat badge.
- Badges: pill radius 30px (scales up to ~9vw on mobile), bg `#82FFCD`, bold ~600 weight, tiny font (9–13px).
- Final line carries the intro paragraph + 3rd video slot.
- Contact details bottom-right on desktop, becomes absolutely positioned corner block on mobile per source CSS.
